version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build    # next build && next export
  artifacts:
    baseDirectory: out    # Next.js export folder
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
